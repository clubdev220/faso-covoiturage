import 'package:flutter/material.dart';

class TripSearchScreen extends StatelessWidget {
  const TripSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Trip Search Screen'),
      ),
    );
  }
}